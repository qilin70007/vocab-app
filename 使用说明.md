# 中考词汇背诵助手使用说明

## 每天怎么用

1. 首页查看“今日目标”和到期复习数量。
2. 进入“背词”，默认生成 40 个未背过单词。
3. 先回忆，再显示释义，然后选择“不认识 / 模糊 / 认识”。
4. 进入“复习”，处理系统安排的到期词和错题。
5. 用“拼写”完成中文到英文的输出训练。

## 标记含义

- **未背过**：完全不会或还没有学过。
- **学习中**：有印象，但不能稳定说出词义或拼写。
- **已掌握**：能够迅速说出含义，并能在句子中识别或使用。

“不认识＋模糊”筛选会集中显示前两类单词，适合快速反复背诵。

## 间隔复习

复习时先回忆答案，再选择：

- **重来**：10 分钟后再次出现。
- **困难**：较短间隔后复习。
- **记得**：按正常间隔增加复习时间。
- **很熟**：使用更长复习间隔。

## 手机端怎么用

### 方式一：手机浏览器访问电脑网址

1. 电脑运行 `npm start`。
2. 手机和电脑连接同一 Wi-Fi。
3. 电脑终端会显示 `手机访问: http://192.168.x.x:3000`，用安卓 Chrome/Edge 打开它。
4. 手机和电脑填写同一个同步码。
5. 这种方式依赖电脑服务器：电脑不在身边、服务没开、或不在同一网络时，手机网址就打不开。

### 方式二：安装真正手机端 APK

如果希望电脑不在身边也能背词，需要生成 APK。仓库里的手机端入口在 `public/mobile.html`，内置词库在 `public/words.json`，打包配置在 `capacitor.config.json`；生成 APK 后通常在 `android/app/build/outputs/apk/debug/app-debug.apk`。完整步骤见 `docs/android.md`。

APK 打开后可直接背词、复习、拼写；进度保存在手机本地。需要和电脑交换进度时，先用“同步设置”的导出/导入 JSON 备份完成合并。

## 数据备份

“同步设置”支持导出和导入 JSON 备份。建议每周导出一次。导入采用合并方式，以更新时间较新的单词记录为准。
